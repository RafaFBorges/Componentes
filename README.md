# Componentes

Kit de interface reutilizável extraído do Minhas Contas Web: botão, texto, link, toggle, input, telefone, tema claro/escuro e validações.

Uso no app (submódulo):

```ts
import { ThemeProvider, ThemeButton, StyledInput } from '@rafafborges/componentes'
```
